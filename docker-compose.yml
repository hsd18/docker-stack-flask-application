version: "3.9"

services:
  web:
    image: stackdemo
    networks:
      - skynet
    build: .
    ports:
      - target: 8000
        published: 8000
        mode: host
  redis:
    image: redis:alpine
    networks:
      - skynet
      
networks:
  skynet:
